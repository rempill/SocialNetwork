package domain;

/**
 * Marker capability for ducks that can swim and thus are eligible for race events.
 */
public interface Inotator {
    /** Perform a swimming action (behavior placeholder). */
    void inoata();
}
