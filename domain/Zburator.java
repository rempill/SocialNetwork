package domain;

/**
 * Capability for ducks that can fly. Not directly used by races, but informs duck type.
 */
public interface Zburator {
    /** Perform a flying action (behavior placeholder). */
    void zboara();
}
